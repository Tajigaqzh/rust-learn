# Rust Learn 协作规范

## 新增章节清单

实现一个新章节时，必须同步完成以下内容：

1. 在 `src/` 下新增章节模块，命名为 `rustNN_主题/mod.rs`。
2. 在 `src/main.rs` 中声明模块、导入公开演示函数，并在 `main` 中调用，保持章节顺序。
3. 在 `docs/guide/` 下新增正文，命名为 `rustNN-主题.md`；正文应说明概念、命令、示例输出和练习，并与实际代码保持一致。
4. 在 `docs/guide/index.md` 的章节规划表中，将该章节从“规划中”改为正文链接和配套代码路径。
5. 在 `docs/.vitepress/config.mts` 的导航和侧边栏中加入可点击链接，不能只保留“规划中”文本。
6. 在 `README.md` 的章节进度表中同步章节标题、正文链接和代码目录。
7. 如果引入依赖、feature、benchmark 或其他 Cargo 配置，必须同步修改 `Cargo.toml`，并提交相应的 `Cargo.lock` 变更。
8. 为公开演示逻辑添加必要的单元测试；涉及跨模块行为时补充 `tests/` 集成测试。

## 编码与文件操作

- 所有 Markdown、Rust、TypeScript 和 TOML 文件必须使用 UTF-8 编码。
- 读取文件时显式指定 UTF-8；写入文件时使用 UTF-8 无 BOM，避免 PowerShell 默认编码造成乱码。
- 修改文档时优先使用小范围补丁，不要为了改一行而整体转换或重写已有文件。
- 不得把 PowerShell 的 `` `r``、`` `n`` 或 `` `r`n`` 字面量写入 Markdown、配置或源码。
- 修改后检查中文内容、换行和 Markdown 表格列数，确认没有 `�` 等替换字符。

## 验证要求

完成章节后至少执行：

```bash
cargo fmt --check
cargo test
cargo run
```

如果修改了 VitePress 配置或文档导航，还应执行：

```bash
pnpm docs:build
```

构建因环境权限或网络原因失败时，必须在交付说明中明确指出，不得把未验证的结果表述为构建成功。

## 代码风格

- 遵循 Rust 2024 edition 和 `rustfmt` 默认格式；不要手工维护与 `cargo fmt` 冲突的排版。
- 模块名、函数名和变量名使用 `snake_case`，类型、trait 和枚举使用 `PascalCase`，常量使用 `SCREAMING_SNAKE_CASE`。
- 演示函数保持短小、按主题分段输出；复杂逻辑拆成可测试的私有函数，避免把所有代码堆在 `*_demo` 中。
- 优先使用标准库和仓库已经存在的依赖，不为简单示例引入新的抽象或第三方库。
- 错误处理优先返回 `Result`/`Option`；只有示范 panic 行为或测试断言时才使用 `unwrap`、`expect` 或 `panic!`，并写清原因。
- 不使用未使用的导入、变量或死代码；公开 API 应有清晰的可见性边界。

## 注释与正文风格

- 注释解释“为什么”、边界条件和 Rust 规则，不重复代码表面行为。
- 模块级文档使用 `//!`，公开函数、类型和重要示例使用 `///`；注释语言与现有章节保持一致，当前仓库以中文为主。
- 注释应短而具体，避免空泛的“定义变量”“调用函数”式旁白；复杂算法或不直观的生命周期/并发约束前可加一段导向说明。
- 正文中的代码、命令和输出必须可由当前仓库复现；涉及版本、性能或编译行为时，以实际命令结果为准。
- Markdown 标题层级连续，表格列数一致；章节正文通常包含目标、示例、常见错误、练习和小结。

## 修改范围与协作

- 只修改完成当前任务所必需的文件；不要顺手重排旧章节、升级无关依赖或格式化整个仓库。
- 发现用户已有改动时先读取并在其基础上修改，不得使用 `git reset --hard`、`git checkout --` 等命令覆盖它们。
- 文档导航、章节进度、Cargo 配置和代码入口属于同一变更，必须保持一致；不要只改其中一处。
- 修改配置或文档编码时，先确认原文件编码，再做局部 UTF-8 修改；发现乱码应从可靠的 UTF-8 来源恢复后再补丁更新。
- 交付说明应列出实际修改的文件、验证命令及任何未完成或受环境限制的检查。

## FFI 章节规范

- 讲解 FFI 时必须同时说明调用约定（如 `extern "C"`）、参数的 C ABI 类型、结构体布局（`#[repr(C)]`）、指针与长度的传递方式，以及库的编译/链接来源（`build.rs`、`cc` 或系统库）。
- 每个跨语言指针接口都要写清空指针语义、长度边界、生命周期、所有权与释放责任；不得只给出一个裸 `extern` 声明而省略契约。
- 字符串示例必须区分 `CString`/`CStr`、NUL 结尾和编码问题；切片示例必须显式传递指针与长度。
- FFI 边界不得让 Rust panic 或 C++ 异常穿透；应展示错误码、out 参数或安全包装函数，并说明线程安全和回调注销规则。
- `unsafe` 示例必须最小化范围，在每个块前写 `SAFETY` 注释，并为前置条件、空指针、越界和错误路径添加测试或明确验证命令。
- 涉及外部 C 库时，正文应说明目标平台、静态/动态链接差异、构建工具链和发布时的动态库部署要求。
- FFI 不限于 C：条件允许时应至少补充一种其他语言（例如 Python `ctypes`、C# P/Invoke 或 Go `cgo`）的调用示例，说明该语言如何加载动态库、声明 ABI 参数和维持缓冲区生命周期；复杂对象优先推荐成熟绑定框架，并解释其适用边界。
